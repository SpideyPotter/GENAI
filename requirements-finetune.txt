# Fine-tuning stack (QLoRA + SFT). Install PyTorch with a CUDA wheel matching the pod first:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# Then:
#
#   pip install -r requirements-finetune.txt
#
# If the image has RAPIDS cuDF and pip reports a pyarrow conflict after installing datasets,
# either use a clean venv for fine-tuning only, or align pyarrow before datasets, e.g.:
#   pip install 'pyarrow>=16.1.0,<16.2.0a0'

transformers>=4.44.0
datasets>=2.19.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
bitsandbytes>=0.43.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.25.0
scipy>=1.11.0
